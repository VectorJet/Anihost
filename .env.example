# Public API base used by the browser. `/api/v1` works with Next.js rewrites.
NEXT_PUBLIC_API_URL=/api/v1

# Optional server-only override used by server actions.
API_BASE_URL=http://127.0.0.1:4001/api/v1
